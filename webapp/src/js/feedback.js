// feedback.js — Owner: Member C — Real-time UI feedback rendering for notes and posture

function showNote(noteName, correct) {
    // TODO: display noteName in #note-display, apply correct/incorrect styling
}

function showTimingError(ms) {
    // TODO: render timing offset in #timing-bar (positive = late, negative = early)
}

function showPostureAlert(className) {
    // TODO: display posture class warning in #posture-alert (hide if className === 'correct')
}

function clearFeedback() {
    // TODO: reset #note-display, #timing-bar, #posture-alert to idle state
}
